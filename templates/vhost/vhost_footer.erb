}

<% if rewrite_www %>
server {
  listen                <%= listen_ip %>;
  server_name           www.<%= server_name.gsub(/^www\./, '') %>;
  rewrite               ^ http://<%= server_name.gsub(/^www\./, '') %>$uri permanent;
}
<% end %>